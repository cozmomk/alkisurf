{
  "name": "alkisurf",
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "build": "cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
